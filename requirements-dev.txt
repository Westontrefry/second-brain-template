-r requirements.txt
pytest>=8.0
# Cockpit (Arc B rung 2): server + FastAPI's TestClient dep for tests.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
# Lint + typecheck gates (CI-only, never imported at runtime).
ruff>=0.6
mypy>=1.11
